---
phase: 2-upload-status
plan: 02
files_modified:
  - public/index.html
  - tests/public-config.test.ts
requirements:
  - ห้ามส่ง upload request ก่อน LIFF initialization และ access token พร้อม
  - รองรับ login redirect แล้วกลับมาใช้งานต่อได้
  - แจ้งสถานะ authentication ที่เข้าใจง่าย
  - ห้าม fallback เป็น dummy identity ใน production
  - ห้ามเปิดเผย access token หรือ secret
must_haves:
  - upload รอ auth readiness ก่อนเริ่ม request
  - กรณีไม่มี LIFF/access token ไม่ยิง API และแจ้งให้เปิดผ่าน LINE/ลองใหม่
  - ปุ่มเลือกไฟล์ disabled ระหว่าง auth initialization และเปิดเมื่อพร้อม
  - tests ครอบคลุม auth guard
---

<objective>
แก้ production bug ที่ upload ถูกส่งก่อน LIFF access token พร้อม ทำให้ backend ตอบ 401 ต้องเข้าสู่ระบบ LINE ก่อนใช้งาน
</objective>

<tasks>
1. ตรวจ flow LIFF init/login และแยก promise/state สำหรับ auth readiness
2. ปรับ upload และ save handlers ให้รอ auth พร้อมและไม่ส่ง request เมื่อไม่มี access token
3. ปรับ UI state/ข้อความและการ enable-disable file input ให้ผู้ใช้เข้าใจและลองใหม่ได้
4. เพิ่ม regression tests ตรวจว่ามี auth guard และไม่มี dummy identity
5. รัน npm test, npm run build, git diff --check, ตรวจ secret, commit และ push origin main; ห้าม deploy

</tasks>

<verification>
- npm test ผ่าน
- npm run build ผ่าน
- git diff --check ผ่าน
- live unauthenticated request ยังคงถูกปฏิเสธ 401
- ไม่มี token/secret ใน source
- ไม่ deploy production
</verification>
